(function(win) {

	//初始化接口
	var InteractiveActivity = function(_api) {
		init(win)
	}


	var p = InteractiveActivity.prototype; //创建接口原型
	//初始化
	function init(win) {
		var main = new mainpath()
	}



	win.InteractiveActivity = InteractiveActivity;

})(window)